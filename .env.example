# ===========================================
# ROOT ENV FILE - FOR REFERENCE ONLY
# ===========================================
# This file shows ALL environment variables used in the project.
# Copy the relevant variables to each app's .env file.
#
# DO NOT commit real credentials to git!
# ===========================================

# ----- SHARED: Database (Neon) -----
# Used by: packages/database/.env AND apps/api/.env
DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# ----- API Server (apps/api/.env) -----
PORT=3001
NODE_ENV=development
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars
CLIENT_URL=http://localhost:3000

# Storage (S3)
STORAGE_PROVIDER=s3
STORAGE_BUCKET=your-bucket-name
STORAGE_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIAXXXXXXXXXXXXXXXX
AWS_SECRET_ACCESS_KEY=your-secret-key

# ----- Web Frontend (apps/web/.env) -----
NEXT_PUBLIC_API_URL=http://localhost:3001
